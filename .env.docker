# Docker Environment Configuration
# This file is used when running with docker-compose
# Copy this to .env.docker.local and customize as needed

# Server Configuration
NODE_ENV=production
PORT=3014
API_PREFIX=/api/v1

# Database Configuration (Docker)
# Note: DB_HOST is set to the external database IP
DB_HOST=35.216.155.219
DB_PORT=5432
DB_NAME=searle_dw
DB_USER=searledw
DB_PASSWORD=searledw$$$$122024$$$$
DB_DIALECT=postgres
DB_POOL_MAX=5
DB_POOL_MIN=0
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# JWT Configuration
JWT_ACCESS_SECRET=your-access-token-secret-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-token-secret-change-this-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# CORS Configuration
CORS_ORIGIN=http://192.168.130.72:3014,http://localhost:3014
CORS_CREDENTIALS=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
